export interface currentUser {
  valid_user: boolean;
  status: string;
  email: string;
  token: string;
}
export interface LoginData {
  email: string;
  password: string;
}
export const _dummy = undefined;
